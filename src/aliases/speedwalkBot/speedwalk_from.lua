walker_current_path = matches[2]
            -- the backward stuff is more work than it is worth so just always going forward
			mushclientspeedwalk(speedwalkBot.paths['from'..walker_current_path],false)